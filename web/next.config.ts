import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["lh3.googleusercontent.com", "picsum.photos", "i.pinimg.com"],
  },
  reactStrictMode: false,
};

export default nextConfig;
